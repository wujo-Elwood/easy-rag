<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rag.mapper.ChatMessageMapper">

    <!-- 根据会话编号查询聊天消息列表 -->
    <select id="findBySessionId" resultType="com.rag.entity.ChatMessage">
        SELECT *
        FROM ai_chat_message
        WHERE session_id = #{sessionId}
        ORDER BY create_time
    </select>

    <!-- 新增聊天消息 -->
    <insert id="insert" parameterType="com.rag.entity.ChatMessage" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_chat_message (session_id, role, content, create_time)
        VALUES (#{sessionId}, #{role}, #{content}, NOW())
    </insert>

    <!-- 根据会话编号删除聊天消息 -->
    <delete id="deleteBySessionId">
        DELETE FROM ai_chat_message WHERE session_id = #{sessionId}
    </delete>

</mapper>
